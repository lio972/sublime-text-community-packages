This Package is marked for deprecation ( and will be deleted ) It was originally intended as a Plugin `meta package`.


It contained two plugins ( put in here as they were `Plugins` ) which are now available elsewhere:

Search.py  => SearchInOpenViews package

Zoom.py  => Zoom package


Please continue development there as this package is marked for deletion.