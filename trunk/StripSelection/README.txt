StripSelection plugin

	This command removes leading and trailing whitespace from each current selection.
	
	It will not modify the file - it'll simply reduce the selection.
	
	Default shortcut is Ctrl+Alt+L.